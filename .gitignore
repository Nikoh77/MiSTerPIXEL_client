# Runtime files — never commit (per-device token + web-managed/local state)
pixel_remote.ini
pixel_token
pixel.log
pixel.pid
pixel.version

# Local-only dev notes (kept in Italian, not published)
instructions.md

# Python
.venv/
__pycache__/
*.pyc
.mypy_cache/
